import QtQuick
import QtQuick.Controls
import Notera

MenuSeparator {
    implicitHeight: 11
    leftPadding: 9
    rightPadding: 9
    contentItem: Rectangle {
        implicitHeight: 1
        color: Theme.border
    }
}
